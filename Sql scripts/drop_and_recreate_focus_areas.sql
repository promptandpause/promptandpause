-- ============================================================================
-- DROP AND RECREATE FOCUS AREAS TABLE (FRESH START)
-- ============================================================================
-- Use this ONLY if you want to completely start over and lose any existing data
-- ============================================================================

-- Drop the table if it exists
DROP TABLE IF EXISTS public.focus_areas CASCADE;

-- Now run the full creation script
-- (Copy and paste the contents of create_focus_areas_table.sql here)
